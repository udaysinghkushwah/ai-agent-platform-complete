export { createClient, AapClient, Trace, SpanHandle } from './client';
export type { ClientOptions, SpanEvent, EventType } from './types';
export type { StartTraceInput, StartSpanInput, SpanEndInput } from './client';
